package com.example.taskmanagement.mcp;

// Deprecated combined models file. Separate classes now exist:
// JsonRpcRequest, JsonRpcResponse, JsonRpcError
// This file retained to avoid stale references; can be deleted once confirmed unused.

public final class JsonRpcModels { private JsonRpcModels() {} }
